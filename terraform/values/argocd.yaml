---
global:
  image:
    tag: "v2.6.6"

server:
  extraArgs:
    - --insecure

redis:
  enabled: true
  auth:
    enabled: false
  resources:
    requests:
      memory: "1Gi"
      cpu: "500m"
    limits:
      memory: "2Gi"
      cpu: "1"